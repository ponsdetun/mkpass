# mkpass

A small, dependency-free command-line password generator.

Built on Python's `secrets` module for cryptographically strong randomness.
Standard library only — no pip install, just run it.

## Status

Early work in progress.
